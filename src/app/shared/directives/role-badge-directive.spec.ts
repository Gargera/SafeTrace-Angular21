import { RoleBadgeDirective } from './role-badge-directive';

describe('RoleBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleBadgeDirective();
    expect(directive).toBeTruthy();
  });
});

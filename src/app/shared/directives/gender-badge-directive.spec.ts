import { GenderBadgeDirective } from './gender-badge-directive';

describe('GenderBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new GenderBadgeDirective();
    expect(directive).toBeTruthy();
  });
});

import { CaseStatusBadgeDirective } from './case-status-badge-directive';

describe('CaseStatusBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new CaseStatusBadgeDirective();
    expect(directive).toBeTruthy();
  });
});

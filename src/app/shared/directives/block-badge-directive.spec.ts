import { BlockBadgeDirective } from './block-badge-directive';

describe('BlockBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockBadgeDirective();
    expect(directive).toBeTruthy();
  });
});

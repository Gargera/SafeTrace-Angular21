import { AgeBadgeDirective } from './age-badge-directive';

describe('AgeBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeBadgeDirective();
    expect(directive).toBeTruthy();
  });
});

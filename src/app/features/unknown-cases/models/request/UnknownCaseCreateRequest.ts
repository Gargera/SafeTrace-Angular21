import { CaseCreateBaseRequest } from "../../../../core/models/Cases.model";

export interface UnknownCaseCreateRequest extends CaseCreateBaseRequest {
  // Urgent case specific fields - add here if any
}
import { CasesFilterRequest } from "../../../../core/models/Cases.model";

export interface UnknownCaseFilterRequest extends CasesFilterRequest {
  // Unknown case specific filter fields - add here if any
}
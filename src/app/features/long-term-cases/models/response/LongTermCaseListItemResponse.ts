import { CaseListItemResponse } from "../../../../core/models/Cases.model";

export interface LongTermCaseListItemResponse extends CaseListItemResponse {
  // Long term case specific list item fields - add here if any
}
import { CaseListItemResponse } from "../../../../core/models/Cases.model";

export interface UrgentCaseListItemResponse extends CaseListItemResponse {
  // Urgent case specific list item fields - add here if any
}
